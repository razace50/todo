import { Button } from "@/components/ui/button";

const NextButton = () => {
  return (
    <div>
      <Button variant={"outline"} type="submit">
        Next page
      </Button>
    </div>
  );
};

export default NextButton;
