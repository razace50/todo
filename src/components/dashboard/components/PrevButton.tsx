import { Button } from "@/components/ui/button";

const PrevButton = () => {
  return (
    <div>
      <Button variant={"outline"} type="submit">
        PreviousPage
      </Button>
    </div>
  );
};

export default PrevButton;
